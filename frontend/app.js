const C=document.getElementById('content'),T=document.getElementById('title'),U=document.getElementById('updated');let chart;
const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const num=x=>x==null?'—':Number(x).toLocaleString(undefined,{maximumFractionDigits:2});
const money=x=>x==null?'—':Number(x).toLocaleString(undefined,{maximumFractionDigits:8});
async function api(u){const r=await fetch(u);if(!r.ok){let m='Unable to load this module right now.';try{const j=await r.json();m=j.detail||m}catch{}throw Error(m)}return r.json()}
function setLoading(text='Loading intelligence…'){C.innerHTML=`<div class="loading"><div class="spinner"></div><b>${text}</b><span>Fetching the latest available data</span></div>`}
function card(label,value,sub=''){return `<div class="card metric-card"><div class="muted">${label}</div><strong>${value}</strong><span>${esc(sub)}</span></div>`}
function pill(a){let cls=['BULLISH','BUY BIAS','STRONG BUY','STRONG BULLISH'].includes(a)?'bull':['BEARISH','SELL BIAS','STRONG SELL','STRONG BEARISH'].includes(a)?'bear':'neutral';return `<span class="pill ${cls}">${esc(a)}</span>`}
function setUpdated(){U.textContent='LIVE • '+new Date().toLocaleTimeString()}
async function overview(){T.textContent='Market Overview';setLoading();try{let x=await api('/api/overview');setUpdated();let movers=x.top_movers||[];C.innerHTML=`<div class="hero"><div><div class="eyebrow">LIVE MARKET PULSE</div><h2>What is moving now</h2><p>Quantitative market context across the most active USDT spot pairs.</p></div><button class="ghost" onclick="overview()">↻ Refresh</button></div><div class="grid">${card('Tracked pairs',num(x.pairs),'Binance USDT spot universe')}${card('Top volume',x.top_volume?.[0]?.symbol||'—','Highest 24h quote volume')}${card('Largest move',movers[0]?.symbol||'—',movers[0]?Number(movers[0].priceChangePercent).toFixed(2)+'% 24h':'—')}${card('Data status','LIVE','Public market feed')}</div><div class="two"><section class="panel"><div class="panel-head"><h3>Market movers</h3><span class="muted">24H</span></div><canvas id="marketChart"></canvas></section><section class="panel"><div class="panel-head"><h3>Research stack</h3><span class="tag">v2.5</span></div><div class="stack"><div><b>Technical engine</b><span>Trend, momentum, volume, volatility</span></div><div><b>On-chain intelligence</b><span>Transfers, whales, holders, liquidity and risk</span></div><div><b>Signal fusion</b><span>Market + blockchain evidence in one research view</span></div><div><b>Macro & equities</b><span>NFP, stocks and news adapters</span></div></div></section></div>`;if(chart)chart.destroy();chart=new Chart(document.getElementById('marketChart'),{type:'bar',data:{labels:movers.slice(0,10).map(x=>x.symbol),datasets:[{label:'24h %',data:movers.slice(0,10).map(x=>+x.priceChangePercent)}]},options:{responsive:true,plugins:{legend:{display:false}}}})}catch(e){C.innerHTML=`<div class="empty"><b>Market feed unavailable</b><span>${esc(e.message)}</span><button onclick="overview()">Try again</button></div>`}}
async function fusedSignal(sym){sym=sym.toUpperCase().trim();T.textContent=`${sym} — Live Signal Fusion`;setLoading('Fusing market + technical + on-chain evidence…');try{let r=await api(`/api/crypto/${encodeURIComponent(sym)}/fused`);setUpdated();let c=r.components||{};C.innerHTML=`<div class="hero"><div><div class="eyebrow">LIVE SIGNAL FUSION</div><h2>${esc(r.symbol)}</h2><p>Unified quantitative research from live market and technical evidence. On-chain data can be added by supplying a token contract.</p></div><div class="scorebox">${pill(r.bias)}<strong>${Math.round(r.score)}<small>/100</small></strong><span>${r.confidence}% model confidence</span></div></div><div class="grid">${card('Trend',num(c.trend),'Technical')}${card('Momentum',num(c.momentum),'Technical')}${card('Volume',num(c.volume),'Technical')}${card('On-chain',num(c.onchain),'Default neutral unless token supplied')}</div><section class="panel"><div class="panel-head"><h3>Fusion components</h3><span class="tag">LIVE</span></div><div class="bars">${Object.entries(c).map(([k,v])=>`<div class="barrow"><span>${esc(k)}</span><div class="bar"><i style="width:${Math.min(100,Math.max(0,Number(v)||0))}%"></i></div><b>${num(v)}</b></div>`).join('')}</div></section><section class="panel"><div class="panel-head"><h3>Technical evidence</h3><span class="muted">${esc(r.interval||'1h')}</span></div><div class="grid">${card('Price','$'+money(r.raw?.price??r.price),'Latest')}${card('7D return',num(r.raw?.ret_7d)+'%','Recent')}${card('30D return',num(r.raw?.ret_30d)+'%','Recent')}${card('RSI',num(r.raw?.rsi),'Momentum')}</div></section><div class="notice"><b>Research-only:</b> This signal is an analytical score, not a trading instruction or guarantee.</div>`}catch(e){C.innerHTML=`<div class="empty"><b>Fusion unavailable</b><span>${esc(e.message)}</span><button onclick="liveTerminal()">Back</button></div>`}}
function liveTerminal(){T.textContent='Live Signal Terminal';C.innerHTML=`<div class="hero"><div><div class="eyebrow">MARKET + TECHNICAL INTELLIGENCE</div><h2>Live signal terminal</h2><p>Run the unified fusion engine against live Binance market data.</p></div></div><section class="panel"><div class="panel-head"><h3>Analyze asset</h3><span class="tag">FREE DATA</span></div><div class="inline"><input id="fusionSymbol" value="BTCUSDT" placeholder="BTCUSDT"><select id="fusionInterval"><option>1h</option><option>15m</option><option>5m</option><option>4h</option><option>1d</option></select><button onclick="fusedSignal(document.getElementById('fusionSymbol').value)">Run fusion</button></div></section><section class="panel"><div class="panel-head"><h3>How the score works</h3></div><div class="stack"><div><b>Trend · 25%</b><span>EMA structure and directional regime</span></div><div><b>Momentum · 20%</b><span>RSI and MACD confirmation</span></div><div><b>Volume · 15%</b><span>Current activity versus baseline</span></div><div><b>Volatility · 10%</b><span>Risk-adjusted market conditions</span></div><div><b>On-chain · 30%</b><span>Token intelligence when a contract is supplied; otherwise neutral</span></div></div></section>`}
function signal(sym,h){fusedSignal(sym)}
function onchain(){T.textContent='On-Chain Intelligence';C.innerHTML=`<div class="hero"><div><div class="eyebrow">MULTI-CHAIN TOKEN INTELLIGENCE</div><h2>Token intelligence terminal</h2><p>Transfers, whale activity, observed holder concentration, DEX liquidity and contract heuristics.</p></div></div><section class="panel"><div class="panel-head"><h3>Token analyzer</h3><span class="tag">READ ONLY</span></div><div class="inline"><select id="chain"><option>ethereum</option><option>base</option><option>arbitrum</option><option>optimism</option><option>polygon</option><option>bnb</option></select><input id="address" placeholder="Token contract 0x…"><button onclick="loadToken()">Analyze token</button></div><div id="tokenResult" class="result muted">Paste a token contract address to begin.</div></section><section class="panel"><div class="panel-head"><h3>Address / transaction tools</h3></div><div class="inline"><input id="wallet" placeholder="Wallet address 0x…"><button onclick="loadWallet()">Analyze address</button><input id="txHash" placeholder="Transaction hash 0x…"><button onclick="loadTx()">Lookup tx</button></div><div id="auxResult" class="result muted">Read-only tools. No wallet connection or signing.</div></section>`}
async function loadToken(){const chain=document.getElementById('chain').value,a=document.getElementById('address').value.trim();if(!a)return;C.innerHTML=`<div class="loading"><div class="spinner"></div><b>Analyzing token intelligence…</b><span>Transfers • whales • holders • liquidity • contract risk</span></div>`;try{const r=await api(`/api/onchain/${chain}/token/${encodeURIComponent(a)}`);setUpdated();renderToken(r)}catch(e){C.innerHTML=`<div class="empty"><b>Token analysis unavailable</b><span>${esc(e.message)}</span><button onclick="onchain()">Back</button></div>`}}
function renderToken(r){const tr=r.transfers?.transfers||[],w=r.whales?.whale_transfers||[],h=r.holders?.top_holders||[],pairs=r.liquidity?.pairs||[],flags=r.risk?.flags||[];T.textContent=`${(tr[0]?.symbol||'TOKEN')} — On-Chain Intelligence`;C.innerHTML=`<div class="hero"><div><div class="eyebrow">${esc(r.chain.toUpperCase())}</div><h2>${esc(r.token)}</h2><p>Observed on-chain activity and DEX data. Holder figures are estimates from the indexed transfer window.</p></div><div class="scorebox">${r.risk?.risk_level==='review'?pill('REVIEW'):pill('NO OBVIOUS FLAGS')}<strong>${num(r.activity_summary?.transfer_count)}</strong><span>transfers observed</span></div></div><div class="grid">${card('Transfers',num(r.activity_summary?.transfer_count),'Indexed token transfers')}${card('Wallets',num(r.activity_summary?.unique_wallets),'Unique addresses observed')}${card('DEX liquidity','$'+num(r.liquidity?.total_liquidity_usd),'Available pair liquidity')}${card('Contract bytes',num(r.risk?.bytecode_bytes),'Runtime bytecode')}</div><div class="two"><section class="panel"><div class="panel-head"><h3>Whale activity</h3><span class="muted">Largest observed transfers</span></div><div class="tablewrap"><table><thead><tr><th>Amount</th><th>From</th><th>To</th><th>Block</th></tr></thead><tbody>${w.slice(0,12).map(x=>`<tr><td><b>${num(x.amount)}</b> ${esc(x.symbol)}</td><td>${short(x.from)}</td><td>${short(x.to)}</td><td>${num(x.block)}</td></tr>`).join('')||'<tr><td colspan="4">No indexed whale transfers returned.</td></tr>'}</tbody></table></div></section><section class="panel"><div class="panel-head"><h3>Observed holders</h3><span class="muted">From transfer window</span></div>${h.slice(0,10).map((x,i)=>`<div class="holder"><b>#${i+1} ${short(x.address)}</b><span>${num(x.observed_balance)}</span><strong>${Number(x.share_pct||0).toFixed(2)}%</strong></div>`).join('')||'<p class="muted">Holder analytics requires indexed transfer data.</p>'}</section></div><div class="two"><section class="panel"><div class="panel-head"><h3>DEX liquidity</h3><span class="muted">Top pools</span></div><div class="tablewrap"><table><thead><tr><th>DEX</th><th>Pair</th><th>Liquidity</th><th>24h volume</th></tr></thead><tbody>${pairs.slice(0,12).map(x=>`<tr><td><b>${esc(x.dex)}</b></td><td>${esc(x.base)}/${esc(x.quote)}</td><td>$${num(x.liquidity_usd)}</td><td>$${num(x.volume_24h_usd)}</td></tr>`).join('')||'<tr><td colspan="4">No DEX pairs returned for this chain.</td></tr>'}</tbody></table></div></section><section class="panel"><div class="panel-head"><h3>Contract risk scan</h3><span class="tag">HEURISTIC</span></div><div class="riskbox"><b>Risk state:</b> ${esc(r.risk?.risk_level||'unknown')}<br><b>Proxy:</b> ${r.risk?.proxy_detected?'Detected':'Not detected'}</div>${flags.map(f=>`<div class="reason risk">! ${esc(f)}</div>`).join('')||'<div class="reason">✓ No obvious bytecode flags found by this heuristic.</div>'}<p class="muted">This is not a smart-contract security audit.</p></section></div><section class="panel"><div class="panel-head"><h3>Recent transfers</h3><span class="muted">Latest indexed records</span></div><div class="tablewrap"><table><thead><tr><th>Time</th><th>Amount</th><th>From</th><th>To</th><th>Tx</th></tr></thead><tbody>${tr.slice(0,20).map(x=>`<tr><td>${x.timestamp?new Date(x.timestamp*1000).toLocaleString():'—'}</td><td>${num(x.amount)} ${esc(x.symbol)}</td><td>${short(x.from)}</td><td>${short(x.to)}</td><td>${short(x.hash)}</td></tr>`).join('')||'<tr><td colspan="5">No indexed transfers returned.</td></tr>'}</tbody></table></div></section>`}
const short=x=>x?`${String(x).slice(0,8)}…${String(x).slice(-6)}`:'—';
async function loadWallet(){const a=document.getElementById('wallet').value.trim(),chain=document.getElementById('chain').value,out=document.getElementById('auxResult');if(!a)return;out.textContent='Reading address…';try{let r=await api(`/api/onchain/${chain}/address/${encodeURIComponent(a)}`);out.innerHTML=`<div class="grid">${card('Type',r.type,'Address classification')}${card('Balance',num(r.native_balance),chain.toUpperCase())}${card('Transactions',num(r.transaction_count),'Nonce')}${card('Gas',num(r.gas_price_gwei)+' gwei','Current RPC')}</div>`}catch(e){out.textContent=e.message}}
async function loadTx(){const h=document.getElementById('txHash').value.trim(),chain=document.getElementById('chain').value,out=document.getElementById('auxResult');if(!h)return;out.textContent='Looking up transaction…';try{let r=await api(`/api/onchain/${chain}/tx/${encodeURIComponent(h)}`);out.innerHTML=r.ok?`<div class="grid">${card('Status',r.status,'Receipt')}${card('Value',num(r.value_native),chain.toUpperCase())}${card('Block',num(r.block),'Included block')}${card('Gas used',num(r.gas_used),'Receipt')}</div><div class="notice"><b>From:</b> ${esc(r.from)}<br><b>To:</b> ${esc(r.to||'Contract creation')}</div>`:esc(r.message)}catch(e){out.textContent=e.message}}
async function listings(){T.textContent='New Listings Radar';setLoading();try{let r=await api('/api/new-listings?days=30');setUpdated();C.innerHTML=`<div class="hero"><div><div class="eyebrow">DISCOVERY</div><h2>Recently listed assets</h2><p>Assets onboarded to the Binance USDT spot universe in the last 30 days.</p></div></div><section class="panel"><div class="tablewrap"><table><thead><tr><th>Symbol</th><th>Asset</th><th>Listed</th><th></th></tr></thead><tbody>${r.length?r.map(x=>`<tr><td><b>${esc(x.symbol)}</b></td><td>${esc(x.baseAsset)}</td><td>${new Date(+x.onboardDate).toLocaleDateString()}</td><td><button class="mini" onclick="fusedSignal('${esc(x.symbol)}')">Research</button></td></tr>`).join(''):`<tr><td colspan="4">No recent listings returned.</td></tr>`}</tbody></table></div></section>`}catch(e){C.innerHTML=`<div class="empty"><b>Listing feed unavailable</b><span>${esc(e.message)}</span></div>`}}
function stocks(){T.textContent='Equity Intelligence';C.innerHTML=`<div class="hero"><div><div class="eyebrow">EQUITIES</div><h2>Stock research</h2><p>Connect a market-data provider to activate live equity analysis.</p></div></div><div class="grid">${card('Provider','Alpha Vantage','Set ALPHA_VANTAGE_API_KEY')}${card('News','Finnhub','Set FINNHUB_API_KEY')}${card('Execution','OFF','Research-only platform')}${card('AI layer','Optional','OpenAI API key enables narratives')}</div><section class="panel"><div class="panel-head"><h3>Analyze a stock</h3></div><div class="inline"><input id="stockSym" value="AAPL" placeholder="AAPL"><button onclick="loadStock()">Analyze</button></div><div id="stockResult" class="result muted">Enter a ticker to begin.</div></section>`}
async function loadStock(){const s=document.getElementById('stockSym').value.trim(),o=document.getElementById('stockResult');if(!s)return;o.textContent='Loading stock…';try{let r=await api(`/api/stock/${encodeURIComponent(s)}`);o.innerHTML=`<div class="grid">${card('Symbol',r.symbol||s,'')}${card('Price',r.price==null?'—':'$'+money(r.price),'Latest')}${card('Change',r.change==null?'—':num(r.change),'Daily')}${card('Provider',r.provider||'—','Data source')}</div>`}catch(e){o.textContent=e.message}}
function macro(){T.textContent='Macro Intelligence';C.innerHTML=`<div class="hero"><div><div class="eyebrow">MACRO</div><h2>Employment & macro context</h2><p>Use official/free macro data as contextual evidence rather than a trading trigger.</p></div></div><section class="panel"><button onclick="loadMacro()">Load latest employment data</button><div id="macroResult" class="result muted">No data loaded.</div></section>`}
async function loadMacro(){const o=document.getElementById('macroResult');o.textContent='Loading…';try{let r=await api('/api/macro/nfp');o.innerHTML=`<pre>${esc(JSON.stringify(r,null,2))}</pre>`}catch(e){o.textContent=e.message}}
function history(){T.textContent='Signal History';setLoading();api('/api/history').then(r=>{setUpdated();C.innerHTML=`<section class="panel"><div class="panel-head"><h3>Recent signals</h3></div><div class="tablewrap"><table><thead><tr><th>Symbol</th><th>Score</th><th>Label</th><th>Time</th></tr></thead><tbody>${(r||[]).map(x=>`<tr><td>${esc(x.symbol||'—')}</td><td>${num(x.score)}</td><td>${pill(x.label||'NEUTRAL')}</td><td>${esc(x.created_at||'—')}</td></tr>`).join('')||'<tr><td colspan="4">No signals recorded yet.</td></tr>'}</tbody></table></div></section>`}).catch(e=>C.innerHTML=`<div class="empty"><b>History unavailable</b><span>${esc(e.message)}</span></div>`)}
function performance(){T.textContent='Performance';setLoading();api('/api/performance').then(r=>{setUpdated();C.innerHTML=`<div class="grid">${card('Signals',num(r.signals),'Recorded')}${card('Average score',num(r.average_score),'Model output')}${card('Buy calls',num(r.buy_calls),'Research labels')}${card('Sell calls',num(r.sell_calls),'Research labels')}</div><div class="notice"><b>Important:</b> Historical signal statistics are not the same as a verified trading backtest.</div>`}).catch(e=>C.innerHTML=`<div class="empty"><b>Performance unavailable</b><span>${esc(e.message)}</span></div>`)}
function home(){overview()}
function init(){const nav=document.querySelectorAll('[data-view]');nav.forEach(n=>n.addEventListener('click',()=>{nav.forEach(x=>x.classList.remove('active'));n.classList.add('active');const v=n.dataset.view;({overview,signal:()=>fusedSignal('BTCUSDT'),live:liveTerminal,onchain,listings,stocks,macro,history,performance}[v]||overview)()}));overview()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
